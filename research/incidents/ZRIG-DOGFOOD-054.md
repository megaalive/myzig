# ZRIG-DOGFOOD-054 — richer SPA history (V41)

## Context

V24 history was prompt/answer/cost-string only. V41 adds token fields,
collapse, reuse-into-composer, filter, and JSON export — still sessionStorage.

## Do

Open web UI; run turns; filter/export/reuse. Meta: `spa_history_v41`.

## Friction tip

`F-ZRIG-057` / `docs/V41.md`.
